# Codex Agent 工作规范（柠檬树苗）

> 适用范围：本目录。目标是以最小改动交付可调试、可验证的功能；不把小项目做成过度工程。

## 项目上下文

- 阶段：早期原型 / MVP；优先迭代速度，但必须可调试。
- 维护者代码基础有限：所有非显然改动用中文简短说明“改了什么、为什么”。
- 技术栈、启动和测试命令以仓库实际文件为准；缺失时先说明，不编造命令。

## 工作方式

### 先理解，再做最小改动

- 改代码前说明对任务的理解、会影响的文件和成功标准；有实际风险的歧义才提一个简短问题。
- 一次只处理当前目标；不顺手重构、改格式或增加没有需求的抽象、配置和依赖。
- 匹配本地风格，只触及必要文件；清理由本次改动造成的无用代码。
- 调试先基于真实报错、日志和可复现步骤提出“假设 + 验证方法”。同一问题连续两次修复失败时，先补足日志缩小范围，禁止继续盲改。

### 完成标准与交付

- Bug：说明失败场景和修复后的预期；功能：说明用户可见行为；重构：说明保持不变的行为。
- 每次完成后运行最窄而有意义的检查；未运行必须说明原因。核心主干路径（用户进入 → 核心功能 → 得到结果）优先测试，不为一次性脚本追求覆盖率。
- 非平凡任务交付时给出：Assumption、Changed、Verified、Remaining risk；小改动不必套模板。

## 错误处理契约（硬规则）

1. 禁止静默失败：不得使用 `except Exception: pass`、`except: return None`、`catch (e) {}` 等吞错写法。
2. 捕获异常必须保留原始堆栈、记录足够上下文，并重新抛出或返回明确的错误结果。不要为“让测试过/不崩”而加 `try/except`；崩溃是有价值的信号。
3. 只捕获可预期、范围最小的代码；避免用宽泛 `except Exception` 包住大段逻辑。
4. 错误与日志必须包含导致失败的关键值，例如 `user_id`、状态码；对外提示可友好模糊，内部日志必须精确。
5. Python 使用 `logger.exception("场景", extra={...})`；JS/TS 使用 `logger.error({ err, context }, "场景")`，其中 `err` 保持原始 Error 对象。

## 日志与可观测性

- 使用项目既有 logger；新项目中 Python 用 `loguru`，Node/TS 用 `pino`（生产）或 `consola`（开发）。禁止以 `print` / `console.log` 代替日志。
- 日志同时输出终端和 `logs/app.log`，并确保 `logs/` 被 `.gitignore` 忽略。每条包含时间、级别、模块、消息和结构化字段。
- 在边界记录：HTTP/API 入口（方法、路径、脱敏参数）、外部调用（入参摘要、耗时、结果）、关键状态变化与所有捕获异常。循环内、高频心跳和纯计算函数不逐条记录；需要时用 `debug` 或汇总。
- 级别：`debug` 排查细节，`info` 正常里程碑，`warning` 降级/重试/兜底，`error` 功能失败需处理。
- 严禁记录 API key、token、密码、完整身份证/手机号或用户原始上传内容；只保留长度、前后四位或哈希。
- 启动时输出版本、环境与关键配置加载状态；后端提供 `/health`（含依赖连通性）；每个请求生成并贯穿短 `request_id`。
- 前端提供全局 Error Boundary、`window.onerror` / `unhandledrejection` 监听及可见的网络失败提示，不能白屏或静默失败。

## 安全

- 密钥仅从环境变量读取，绝不硬编码、提交或打印。新增依赖所需密钥时同步更新 `.env.example`（只写变量名）。
- SQL 必须参数化，禁止字符串拼接。
- 提交前检查 `.env`、`logs/` 和密钥文件均在 `.gitignore`。

## 仓库与 Git

- 编码任务开始时确认仓库和当前分支；默认在当前分支工作，不自行创建或切换分支。
- 多步骤任务先询问是否需要“已验证的小里程碑提交并推送”；获授权后，每个相关小里程碑在检查通过后再提交与推送。
- 提交只包含当前请求相关改动；提交前说明改动，推送前运行相关检查。
- 禁止强推、重写历史、改 remote/仓库设置/协作者/分支保护或删除分支；无 remote 时如需推送请用户提供配置。

## 删除与范围安全

- 禁止批量、递归、通配符或脚本循环删除。只有必要时，使用直接字面路径一次删除一个明确目标；多个目标或递归清理先征得确认。
- 对无关死代码、设计问题或可能扩大的方案，单独指出，不混入当前补丁；同时给出满足当前目标的更小路径。

## 常用检查（按项目实际补充）

```bash
# 启动开发环境
# <command>

# 运行测试
# <command>

# 查看最近日志（PowerShell）
Get-Content logs/app.log -Tail 200
```
